# Informer

**Informer** — это приложение для Windows, которое отображает системную информацию в виде полупрозрачного оверлея на рабочем столе. Приложение работает в фоновом режиме и предоставляет быстрый доступ к ключевым данным о системе.

![Version](https://img.shields.io/badge/version-1.3.0.0-blue)
![.NET](https://img.shields.io/badge/.NET-Framework%204.6-purple)
![License](https://img.shields.io/badge/license-All%20rights%20reserved-red)

## ✨ Основные возможности

- 📊 **Отображение системной информации** — процессор, память, диск, сеть, ОС
- 🎨 **Настраиваемый интерфейс** — шрифт, цвет, размер, позиция окна
- 🔄 **Автоматическое обновление** — данные обновляются с настраиваемым интервалом
- 🎯 **Экспорт данных для удаленного управления** — интеграция с системами удаленного управления (Aspia и другие)
- 🖥️ **Работа в трее** — иконка в системном трее с контекстным меню
- ⚙️ **Графический редактор настроек** — удобная форма для изменения конфигурации
- 🚀 **Автозапуск** — автоматический запуск при входе пользователя
- 📦 **MSI установщик** — простая установка


## 📋 Системные требования

- **ОС:** Windows 7 или выше
- **.NET Framework:** 4.6 или выше
- **Права:** Установка в профиль пользователя (не требует прав администратора)

## 🚀 Установка

### Через MSI установщик (рекомендуется)

1. Скачайте `informer.gpo.msi` на странице релизов
2. Запустите установщик
3. Следуйте инструкциям мастера установки
4. Приложение автоматически установится в профиль пользователя и запустится

### Ручная установка

1. Скопируйте файлы приложения в нужную папку
2. Настройте автозапуск через реестр или планировщик задач
3. Запустите `Informer.exe`

## ⚙️ Настройка

### Быстрая настройка через интерфейс

1. Кликните правой кнопкой мыши по иконке в системном трее
2. Выберите **"Настройки"**
3. Измените нужные параметры
4. Нажмите **"Сохранить"**

### Ручная настройка через конфиг

Настройки хранятся в файле `Informer.exe.config`. Вы можете редактировать его вручную или через пункт меню **"Открыть конфиг"** в трее.

#### Основные параметры

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `WindowWidth` | Ширина окна (пиксели) | 600 |
| `WindowHeight` | Высота окна (пиксели) | 400 |
| `FontName` | Имя шрифта | Arial |
| `FontSize` | Размер шрифта | 18 |
| `FontBold` | Жирный шрифт | false |
| `FontItalic` | Курсив | false |
| `FontUnderline` | Подчеркнутый | false |
| `TextColor` | Цвет текста (имя или ARGB) | White |
| `ShadowEnabled` | Включить тень | true |
| `ShadowColor` | Цвет тени (ARGB) | 128,0,0,0 |
| `ShadowAlpha` | Прозрачность тени (0-255) | 128 |
| `UpdateInterval` | Интервал обновления (мс) | 7200000 (2 часа) |

#### Настройки экспорта данных для удаленного управления

Для работы с системами удаленного управления необходимо раскомментировать и настроить следующие параметры:

```xml
<!-- Включить экспорт данных -->
<add key="AspiaEnabled" value="true" />

<!-- Путь к JSON файлу на сетевом диске -->
<add key="AspiaNetworkPath" value="\\server\share\computers.json" />

<!-- Список подсетей для фильтрации IP адресов (через запятую) -->
<add key="AspiaNetworkSubnets" value="192.168.1.,192.168.2.,10.0.0." />
```

**Важно:** 
- Если `AspiaNetworkSubnets` не указан или пуст, фильтрация не применяется
- Подсети указываются с точкой в конце (например: `192.168.1.` вместо `192.168.1`)
- Можно указать несколько подсетей через запятую

## 📊 Отображаемая информация

Приложение показывает следующую информацию:

- **Пользователь и компьютер:**
  - Имя пользователя
  - Имя компьютера
  - Домен (если компьютер в домене, не показывается для WORKGROUP)
  - Домен-контроллер (если компьютер в домене)

- **Операционная система:**
  - Версия Windows (например: Windows 11 25H2)
  - Разрядность (32-bit / 64-bit)
  - Номер сборки
  - Время работы системы (uptime) в формате: дни, часы, минуты

- **Сеть:**
  - IP-адреса активных сетевых интерфейсов
  - Имена сетевых адаптеров (LAN, VM, Wi-Fi и т.д.)

- **Диски:**
  - Свободное место на каждом диске (в ГБ)
  - Общий объем диска (в ГБ)
  - Процент использования

## 🔌 Сборщик информации для систем удаленного управления

Приложение может автоматически экспортировать информацию о компьютере в JSON файл для использования с системами удаленного управления (например, [Aspia](https://aspia.org/), RDP-менеджеры и другие).

Этот функционал предназначен для администраторов, которым необходимо централизованно собирать информацию о компьютерах в сети для последующего удаленного управления.

### Как это работает

1. Приложение периодически проверяет изменения в сетевых настройках (IP-адрес, пользователь)
2. При обнаружении изменений обновляет JSON файл на сетевом диске
3. Система удаленного управления читает этот файл и использует данные для подключения к компьютерам

### Формат экспортируемых данных

Приложение экспортирует данные в стандартный JSON формат:

```json
[
  {
    "computerName": "COMPUTER-01",
    "userName": "username",
    "ipAddress": "192.168.1.100",
    "lastUpdate": "2025-01-22T15:30:00"
  }
]
```

Этот формат совместим с большинством систем удаленного управления, которые могут читать JSON файлы.

### Особенности реализации

- ✅ Автоматическое удаление дубликатов записей для одного компьютера
- ✅ Умное обновление существующих записей без потери данных
- ✅ Настраиваемая фильтрация IP-адресов по подсетям
- ✅ Восстановление IP из существующих записей при временной недоступности сети
- ✅ Безопасная запись с обработкой конфликтов доступа к файлу
- ✅ Асинхронная работа без блокировки основного приложения

## 🎯 Использование

### Системный трей

Приложение работает в фоновом режиме и отображается в системном трее. Доступные действия:

- **Двойной клик** — открыть форму настроек
- **Правый клик** — контекстное меню:
  - **Открыть конфиг** — открыть файл конфигурации в блокноте
  - **Настройки** — открыть графический редактор настроек
  - **Закрыть** — завершить работу приложения

### Автоматическое обновление

- Настройки автоматически применяются при изменении конфига
- Не требуется перезапуск приложения
- Изменения вступают в силу сразу после сохранения

## 🛠️ Разработка

### Сборка проекта

```bash
# Сборка Release версии
dotnet build --configuration Release


### Структура проекта

```
public/
├── AspiaExporter.cs          # Экспорт данных для удаленного управления
├── Settings.cs               # Управление настройками
├── SettingsForm.cs           # Форма настроек
├── SystemInfoOverlay.cs      # Основное окно приложения
├── Program.cs                # Точка входа
├── App.config                # Конфигурационный файл
```

## 📝 Версия 1.3.0.0

### Новые возможности

- ✨ Добавлена анимация загрузки с пульсацией прозрачности
- 🎨 Добавлена форма настроек с графическим интерфейсом
- 🔔 Добавлена иконка в системном трее с контекстным меню
- ⚙️ Настраиваемая фильтрация IP-адресов по подсетям для экспорта данных
- 🔄 Улучшена логика удаления дубликатов при экспорте


### Улучшения

- Автоматическое применение изменений конфига без перезапуска
- Улучшенная обработка ошибок
- Оптимизация производительности

## 📄 Лицензия

All rights reserved © 2026 Future Tech

## 🤝 Вклад в проект

Если вы нашли ошибку или хотите предложить улучшение, пожалуйста, создайте Issue в репозитории.

## 📞 Поддержка

При возникновении проблем:

1. Проверьте файл конфигурации `Informer.exe.config`
2. Убедитесь, что .NET Framework 4.6+ установлен
3. Проверьте права доступа к файлам и сетевому диску (для экспорта данных)
4. Создайте Issue с описанием проблемы

---

**Приятного использования!** 🚀
